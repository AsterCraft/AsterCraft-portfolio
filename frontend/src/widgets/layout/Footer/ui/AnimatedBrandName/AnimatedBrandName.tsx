import classNames from "classnames";

const AnimatedBrandName = () => {
  return (
    <h2
      className={classNames(
        "text-center text-[17vw] tracking-tight text-zinc-300",
        "sm:text-[18vw]",
        "2xl:text-[14vw]"
      )}
    >
      ASTERCRAFT
    </h2>
  );
};

export default AnimatedBrandName;
